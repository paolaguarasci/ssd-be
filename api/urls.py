from django.urls import path

# from api.views import BookDetail, BookList, BookLoanDetail, BookLoanList

urlpatterns = [
    # path('books/<int:id>', BookDetail.as_view()),
    # path('books/', BookList.as_view()),
    # path('loans/<int:id>', BookLoanDetail.as_view()),
    # path('loans/', BookLoanList.as_view()),
]